
const initState = {
    username: "",
    provile: "",
    userid: 0
}

reducer(state = initState, action)
{
    switch(action.type)
    {
        default:
        return {...state};
    }
}

export default reducer;